import rubric from './rubrics/template-hypothesis.json';
import { TemplateHypothesisSection } from './TemplateHypothesisSection';

{/* Eksempel på `expected`: validering af navn, symbol og enhed for alle
    fire variabler, med commonMistakes og pædagogiske hints. Navne (xvar,
    yvar) og symboler (X, Y, a, b) matcher det generiske lineære eksempel
    Y = a·X + b — IV/DV er store bogstaver som i ligningen, konstanterne
    er små. Erstat dem med dine egne fysiske størrelser, symboler og
    enheder når du kopierer template'en. Per-celle `hints: string[]`
    udvider den generiske hint-ladder med ekstra senere tier-trin (de
    tilføjes til sidst — de erstatter ikke standard-hintene).

    Når du kopierer template'en til en rigtig lab:
      - Erstat 'variables' med dit eget VariableTable id (samme id i RevealWhen).
      - Tilpas hvilke felter TemplateHypothesisSection indlejrer (her: første IV-/DV-rækkes symbol).
      - RubricResponse skal have `dependsOn` på symbol-parret, så en
        ændring i variabler invaliderer en tidligere evalueret hypotese. */}
export const variablesExpected = {
  iv: {
    name: { accepted: 'xvar' },
    symbol: {
      accepted: 'X',
      hints: [
        'Tjek dit symbol for den uafhængige variabel.',
        'Brug det symbol teorien introducerer for den uafhængige variabel i den lineære sammenhæng Y = a·X + b (stort bogstav).',
      ],
    },
    unit: {
      accepted: 'ux',
      hints: [
        'Tjek enheden for den uafhængige variabel.',
        'Brug den generiske enhed ux som teorien bruger til X.',
      ],
    },
    commonMistakes: {
      symbol: [
        { kind: 'lowercase-x', wrong: 'x',
          hint: 'Variabel-symboler skrives med stort bogstav i denne lab — prøv X.' },
      ],
    },
  },
  dv: {
    name: { accepted: 'yvar' },
    symbol: {
      accepted: 'Y',
      hints: [
        'Tjek dit symbol for den afhængige variabel.',
        'Brug det symbol teorien introducerer for den afhængige variabel i den lineære sammenhæng Y = a·X + b (stort bogstav).',
      ],
    },
    unit: {
      accepted: 'uy',
      hints: [
        'Tjek enheden for den afhængige variabel.',
        'Brug den generiske enhed uy som teorien bruger til Y.',
      ],
    },
    commonMistakes: {
      symbol: [
        { kind: 'lowercase-y', wrong: 'y',
          hint: 'Variabel-symboler skrives med stort bogstav i denne lab — prøv Y.' },
      ],
    },
  },
  constants: [
    {
      name: { accepted: 'hældning' },
      symbol: { accepted: 'a' },
      unit: { accepted: 'ua' },
    },
    {
      name: { accepted: 'skæringspunkt' },
      symbol: { accepted: 'b' },
      unit: { accepted: 'ub' },
    },
  ],
};

En god hypotese bygger på teorien — slå op i Teori øverst hvis du er i tvivl om symbol- og enhedskonventionerne.

<VariableTable
  id="variables"
  iv={{ count: 1 }}
  dv={{ count: 1 }}
  constants={{ count: 2 }}
  requireUnits
  expected={variablesExpected}
  checkInFooter
  checkLabel="Tjek variable"
/>

<RevealWhen widgetIds={['variables']} strict clearOnHide={['hypotese']}>
  <TemplateHypothesisSection rubric={rubric} />
</RevealWhen>
