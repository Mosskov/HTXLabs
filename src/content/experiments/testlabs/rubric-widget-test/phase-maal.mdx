import rubricMini from './rubrics/widget-test-mini.json';
import rubricHypothesis from '../template/rubrics/template-hypothesis.json';

To `<RubricResponse>`-widgets i én fase, ANDet via `all-satisfied`. Verificerer at gate'en kun åbner når begge widgets har `satisfied: true`.

**Probe-tjekliste**
- Pass kun A → gate forbliver lukket (stepper-cirkel for næste fase ikke klikbar).
- Pass også B → gate åbner; Næste-knappen aktiveres.
- Redigér A's tekst → gate lukker igen (Næste deaktiveres); ingen synlig dirty-pille.

<RubricResponse
  id="rubA"
  rubric={rubricMini}
  prompt="Svar A — brug ordet 'test'."
  minWords={3}
/>

<RubricResponse
  id="rubB"
  rubric={rubricHypothesis}
  prompt="Svar B — formulér en kort hypotese om xvar og yvar."
  minWords={6}
/>
