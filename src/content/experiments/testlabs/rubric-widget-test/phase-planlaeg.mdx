import rubric from '../template/rubrics/template-hypothesis.json';

Baseline: standardprops, `rubric-required`-gate. Brug fasen til at verificere tier-progression i hints og gate-overgangen ved pass/dirty.

**Probe-tjekliste**
- Tom tekst → Tjek-knappen er deaktiveret.
- Under 10 ord → knap stadig deaktiveret.
- Mindst 10 ord uden relations-verbum → klik Tjek tre gange; tier-1 → tier-1+2 → tier-1+2+3 (cap).
- Skriv "X stiger, så Y vokser" og klik Tjek → gate åbner (Næste aktiveres), bonus-panel for `principle` dukker op. Skærmlæser annoncerer "Godkendt" via sr-only live-region.
- Redigér teksten efter pass → gate lukker igen (Næste deaktiveres). Ingen synlig dirty-pille — afhængigheden er strukturel.

<RubricResponse
  id="hypotese"
  rubric={rubric}
  prompt="Skriv en kort hypotese om sammenhængen mellem X og Y."
  minWords={10}
/>
